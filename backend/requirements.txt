# --- 그래프/상태 (mock provider 오프라인 실행에 필요) ---
langgraph>=0.2

# --- API 서버 (uvicorn app.main:app) ---
fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.0

# --- 실제 Upstage 연동 (ROOMPILOT_PROVIDER=upstage 일 때만 필요) ---
langchain-upstage>=0.7.7   # solar-pro2 / solar-mini / solar-embedding-1-large

# --- 개발/테스트 (4 게이트: ruff / mypy / pytest / compileall) ---
pytest>=8.0
ruff>=0.5
mypy>=1.10
